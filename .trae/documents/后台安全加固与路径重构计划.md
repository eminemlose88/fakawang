### 后台路径重构与安全加固计划 (修正版)

根据您的指示，我将执行以下操作（**取消 IP 白名单实施**，**执行后台路径重命名**）：

#### 1. 后台路径重命名 (核心任务)
*   **物理重命名**：
    *   将 `app/admin` 目录重命名为 `app/manager`。
    *   将 `app/api/admin` 目录重命名为 `app/api/manager`。
*   **全局引用替换**：
    *   搜索全项目中的 `/admin` 字符串（排除注释和非路径用途）。
    *   将其替换为 `/manager`。
    *   重点检查：
        *   `components/admin/Sidebar.tsx` (链接)
        *   `app/manager/login/page.tsx` (API 调用)
        *   `app/manager/layout.tsx`
        *   `middleware.ts` (如果有)
        *   `lib/auth/session.ts` (如果涉及重定向)

#### 2. Cookie 安全增强
*   **修改 `lib/auth/session.ts`**：
    *   添加 `secure: process.env.NODE_ENV === 'production'`。
    *   添加 `sameSite: 'lax'`。
    *   将硬编码的 `secretKey` 改为优先读取 `process.env.SESSION_SECRET`。

#### 3. Nginx 配置文档
*   创建 `nginx.conf.example`，提供反向代理、静态资源隔离和 Cookie 安全的配置示例（供您部署时参考）。

#### 4. 环境变量
*   更新 `.env.example`，添加 `SESSION_SECRET`。

确认后，我将立即开始执行路径重命名和代码替换。